import { VerticalBox, AboutSlint, Button } from "std-widgets.slint";
export component MainWindow {

    VerticalBox {
        AboutSlint { }
        Text {
            text: "Text";
        }
        Button {
            text: "Button";
        }
    }
}